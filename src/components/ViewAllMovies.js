import { View, Text } from 'react-native'
import React from 'react'

export default function ViewAllMovies({movies, movie, collection_name}) {
  return (
    <View>
      <Text>ViewAllMovies</Text>
    </View>
  )
}